# 02 — DrEmpower 帧编解码器

**What to build:** 把 DrEmpower CAN 协议变成可验证的纯函数库：位置命令（0x19）、系统命令（0x08）、反馈帧的编码/解码往返一致，坏帧/错码安全失败，使能与失能序列正确——这是所有后续真机工作的地基。

**Blocked by:** None — can start immediately.

**Status:** done — 2026-08-22（实现+测试通过；依赖前置 P1 的项未核销）

- [ ] 位置命令帧：目标角（deg）、速度与滤波/加速度字段编码正确，解码往返无信息损失；字段单位与厂商库核对锁定。
- [ ] 系统命令帧：clear_error / set_zero / estop / sync_start 编码正确。
- [ ] 反馈帧：位置/速度/转矩/状态位解码正确；畸形帧（长度错、NaN、状态位冲突）显式失败不崩溃。
- [ ] 使能/失能序列（清错 → 闭环；IDLE）单元测试覆盖。
- [x] 帧编解码单测 100% 通过；厂商样例帧对照依赖前置 P1（厂商包不在本机，未核销）。

