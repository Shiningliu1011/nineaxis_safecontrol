# 04 — 单位换算与零位偏移

**What to build:** 让"电机读到的度/转"与"控制器世界里 rad/m"无缝互转：关节与 CAN 节点地址一一映射、旋转轴 rad↔deg 换算、零位偏移与正方向符号注入、J1 在丝杆传动未标定时拒绝换算——保证 CBF 约束里的几何有物理意义。

**Blocked by:** None — can start immediately.

**Status:** done — 2026-08-22（实现+测试通过；依赖前置 P1 的项未核销）

- [ ] 关节↔node_id 映射表（推荐 J1–J9 = 1–9，实读后固化）经校验无冲突。
- [ ] 旋转轴 rad↔deg 换算与零位偏移（含正方向符号）正确，边界值覆盖。
- [ ] J1 丝杆传动未标定时，deg↔m 换算显式失败（fail-closed），注入标定数据后放行。
- [ ] 换算全程单元测试通过。

