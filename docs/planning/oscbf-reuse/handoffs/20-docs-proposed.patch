--- a/docs/specs/real_robot_landing_spec.md
+++ b/docs/specs/real_robot_landing_spec.md
@@ -37,7 +37,7 @@
 4. 作为现场实验员，我想要锁存的停车原因只能由外部健康确认后人工清除，以便恢复
    流程可审计、可演练。
 5. 作为现场实验员，我想要 J1 在丝杆传动参数实测前被安全网关强制拒绝运动命令
-   （fail-closed），以便避免用错误的 m/s/N 限幅驱动升降轴。
+   （fail-closed），以便避免用错误的 m/s/N 限幅驱动 J1 直线导轨。
 6. 作为现场实验员，我想要 `shadow` 模式（只记录命令与状态、不发送任何 CAN 帧）
    与 `live` 模式一键切换，以便先在记录态验证命令语义再上真机。
 7. 作为集成开发者，我想要先在没有 CAN 硬件的 vcan0 虚拟总线上验证帧编解码、
--- a/config/perception_runtime.yaml
+++ b/config/perception_runtime.yaml
@@ -24,8 +24,9 @@
 #
 # LiDAR is OFF by default (source_topic_lidar: "") to keep single-Camera
 # behavior unchanged and until the calibration gate (T5/T6/T7) is passed; use
-# config/perception_dual_sensor_real.yaml (or dual_sensor_calibrated profile)
-# to enable the dual-sensor runtime.
+# config/perception_dual_sensor_real.yaml as the deployment template. Loading
+# that file alone still leaves LiDAR OFF; enable its source topic only after
+# the calibration gate and SSOT wiring have been implemented and verified.
 #
 # Keep workspace/voxel consistent with
 # portable_oscbf/config/obstacle_params.yaml so the ESDF grid shape matches.
