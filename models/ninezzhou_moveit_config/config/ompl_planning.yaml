# AEB-RRT* Gray Rollout — 2026-08-01
# planning_plugin switched to AEB-RRT* wrapper which registers AEB-RRT*
# AND delegates all other planners to the standard OMPL interface.
# RRTConnect retained as fallback.
#
# ROLLBACK (one-line):
#   planning_plugin: ompl_interface/OMPLPlanner
#   (and move RRTConnectkConfigDefault back to first in arm.planner_configs)
planning_plugin: aeb_rrtstar_ompl/AEBRRTstarPlannerManager
request_adapters: >-
  default_planner_request_adapters/AddTimeOptimalParameterization
  default_planner_request_adapters/ResolveConstraintFrames
  default_planner_request_adapters/FixWorkspaceBounds
  default_planner_request_adapters/FixStartStateBounds
  default_planner_request_adapters/FixStartStateCollision
  default_planner_request_adapters/FixStartStatePathConstraints
start_state_max_bounds_error: 0.1

planner_configs:
  # ---- AEB-RRT* (new default) ----
  AEBRRTstarFaithfulConfigDefault:
    type: geometric::AEBRRTstar
    range: 0.3
    connect_threshold: 0.6
    stop_on_first_solution: true

  AEBRRTstarAnytimeConfigDefault:
    type: geometric::AEBRRTstar
    range: 0.3
    connect_threshold: 0.6
    stop_on_first_solution: false

  # ---- RRTConnect (stable fallback) ----
  RRTConnectkConfigDefault:
    type: geometric::RRTConnect
    range: 0.0

arm:
  planner_configs:
    - AEBRRTstarFaithfulConfigDefault
    - RRTConnectkConfigDefault
    # - AEBRRTstarAnytimeConfigDefault  # enable for anytime optimization
