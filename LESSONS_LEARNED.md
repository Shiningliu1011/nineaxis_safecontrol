# OSCBF 移植经验教训（LESSONS LEARNED）

> 模板：现象 → 根因 → 修复 → 教训。本文档是 `OSCBF_PORTING_GUIDE.md` §11 的
> 项目内落地版，只记录本仓库实际发生并已验证过的条目。

## 内核与 QP

### 弹性 QP 与速率限幅组合崩溃
- **现象**：`JaxControlLoop(rate_limit_du_max=...)` 在 `init_cbf()` 预热时报
  `TypeError: solve_qp_elastic() takes 5 positional arguments but 6 were given`。
- **根因**：M7 把 `relax_cbf=True`，`cbf.qp_solver` 变成 5 参数的
  `qpax.solve_qp_elastic`；速率限幅分支仍按硬 QP 的 6 参数签名调用它。
- **修复**：速率限幅模式（2 个共享速率松弛变量）明确是硬 CBF 构造，改为
  直接调用 `qpax.solve_qp`，并恢复该模式的硬 CBF 健康门（h_ok）。
- **教训**：求解器对象在配置开关下会换签名/换语义；每一个分支都要在
  `relax_cbf` 两种取值下走一遍。配置注释（`soft_rate_limit`：CBF 保持硬、
  仅速率松弛）要与实现分支一一对应。

### 任务一致性 P 在零空间方向近退化
- **现象**：同一代码、同一输入，两次独立编译的运行在首帧就给出
  `u_safe` 相差 0.198 rad/s 的解，3000 步后 `q_next` 漂移 4e-4 rad。
- **根因**：P = NᵀW_joint²N + JᵀW_task²J，零空间方向特征值 ~0.01、任务方向
  100–400，条件数 ~4e4；弹性 PDIP 在 `solver_tol=1e-3` 下，不同 XLA 融合的
  编译会落在最小化解的不同零空间分量上。
- **修复**：跨编译等价性只比较良态观测量（err_6d < 1e-4、路径进度 < 1e-3、
  qp_ok 序列一致），`u_safe`/`q_next` 只记录不设门。
- **教训**：对病态 QP 做“逐位等价”验收没有意义；先确认待比较量是良态的。
  零空间分量本来就不唯一，任务空间分量才是安全控制真正关心的。

### 参考超前量与验收精度直接耦合
- **现象**：稳态 max_ee = 0.1105 mm，超过 §10 的 0.1 mm 阈值；把 kp_pos 从
  50 提到 70 只降到 0.107 mm。
- **根因**：路径跟随只反馈横向误差，沿切向误差上界由 `reference_lead_m`
  （0.1 mm）决定，`reference_lead_m=1e-4` 时误差贴着 0.1 mm 上限走。
- **修复**：`reference_lead_m` 降到 1e-5（投影后积分修正已消除离散时序
  滞后），max_ee 降到 ~0.02–0.04 mm；同时 kp_pos 统一到指南 §6.1 的 60。
- **教训**：追踪精度指标不是单靠反馈增益就能调出来的；路径跟随的“参考点
  超前量”本身就是名义误差的一部分，要先算清误差来源再调参。

## 迁移与验收流程

### 基线产物需要语义标签
- **现象**：M6 等价性门在 M7 切换弹性 QP 后只能 `skip`，因为旧基线不再
  代表当前控制器。
- **根因**：`baseline_tracking.npz` 只存数值，不存生成它的控制器语义；
  阶段之间切换 relax/增益/超前量后，旧产物被静默复用。
- **修复**：npz 里写入 `semantics` 标签，语义变化即递增；下游门先校验
  标签，不匹配时自动重生成基线。
- **教训**：任何跨阶段对照的落盘产物都要带版本/语义标记，否则“通过”或
  “跳过”都可能是在比较两个不同的控制器。

### 模型拓扑改变会连锁打破遗留断言
- **现象**：M2 把 10 球换成 OBB 后，遗留测试报 `assert 10 == 32`。
- **根因**：旧断言把“障碍物行数 == 机器人碰撞球数”当不变式；OBB/DCOL 下
  障碍物按 10 个连杆聚合，而 ESDF 包络球是 32 个。
- **修复**：断言改为当前契约（聚合后每 OBB 连杆一行），保守性检查改为与
  逐槽 DCOL 精确距离对照。
- **教训**：拓扑变化后必须 grep 所有依赖旧计数的测试；用“结构派生”的
  断言（18+14+10+1+…）比魔法数字好。

### 遗留参考测试要跟随语义演进
- **现象**：全量套件从 M0 的 0 失败退化为 3 失败（速率限幅、终端 KKT、
  障碍物行数）。
- **根因**：M7 弹性 QP 移除了硬终端 KKT 健康检查（遥测置 nan/False），
  遗留测试仍断言 `>= 0` / `True`。
- **修复**：按当前语义重写断言并注释原因；受控停车/HARD_STOP 相关旧测试
  保持 `importorskip("newaxis")` 的文档化排除。
- **教训**：参考实现的测试不是永久的契约；目标架构每改一处语义，就要
  同步审计哪些旧测试在测已删除的行为，不能靠 skip 悄悄掩埋。

### JIT 预热预算与实际差距大
- **现象**：完整 `JaxControlLoop` 预热实测 54–97 s，远超 §10 的 30 s。
- **根因**：整块内核首编慢（指南 L94）；M5 阶段门把预算放宽到 120 s，
  而 30 s 门被推到 M6——M6 门一度处于 skip，无人值守。
- **修复**：M6 只量模块化路径内核首编（实测 28.7 s < 30 s），并把完整
  预热作为 M12 待复测项写入验收报告。
- **教训**：把性能门“转交给下一个阶段”时，下一个阶段必须真的执行；
  门的责任链断了就是没有任何门。

## 指南 §11 关键教训（沿用）

| # | 教训 | 后果 |
|---|------|------|
| L1 | 纯 P、无 kd | 显式 Euler 下 kd≥1 发散 |
| L15 | OSCBF 任务一致性 P 必须保留 | 否则避障干扰末端任务 |
| L67 | JAX 边界 = 控制 + 碰撞数值图 | 原始点云/ESDF 构建留在 JAX 外 |
| L70 | qpax warm-start 默认关闭 | 冷启动每步迭代多但更安全 |
| L77 | ESDF dtype 必须 float32 | float64 触发首帧重编译 |
| L79 | 求解器 slack ≠ 物理速率违例 | 弹性 QP 用真实残差判断 |
| L83 | 腕部反向点放大 omega_per_m | 运行时硬上限 ell_dot ≤ 0.15/‖omega_per_m‖ |
| L94 | 单一巨型内核首编慢 | 选模块化 JIT + 单线程 XLA |
| L95 | 初始化瞬态不算稳态 | 验证时区分瞬态与稳态 |
| DCOL | alpha 是逐约束行参数 | 按 V2 DPAX 流程逐对离线标定 |
| 零空间 | 逐关节裁剪破坏 J·qdot_N=0 | 用整体缩放 s_v，限速交给 QP |
